<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />

    <title>Edit <%= workout.name %></title>
</head>
<body>
    <%- include('./partials/nav') %>

    <h1>Edit <%= workout.name %></h1>
    <script
      src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"
      integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r"
      crossorigin="anonymous"
    ></script>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"
      integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy"
      crossorigin="anonymous"
    ></script>

                    <!-- Update workout form -->
    <form class="form" action="/workouts/<%= workout._id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" placeholder="<%= workout.name %>"/>
        <label for="name" >Duration:</label>
    <input type="number" name="duration" placeholder="<%=workout.duration%>"/>
    <div class="checkbox-div">
        <label for="isCompleted">Did you complete your workout?</label>
        <input class="checkbox" type="checkbox" name="isCompleted" <% if (workout.isCompleted) { %> checked <% } %>/>
      </div>
        <button type="submit" class="buttons">Update Workout</button>
    </form>
                     <!-- Exercise cards -->
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
  <% workout.exercises.forEach((exercise) => { %>
    <div class="col">
      <div class="card h-100" style="width: 18rem">
        <div class="card-body">
          <form action="/workouts/<%= workout._id %>/exercises/<%= exercise._id %>?_method=PUT" method="POST">
            <div class="mb-3">
              <label for="name" class="form-label">Exercise Name:</label>
              <input type="text" name="name" class="form-control" placeholder="<%= exercise.name %>"/>
            </div>
            <div class="mb-3">
              <label for="reps" class="form-label">Sets x Reps:</label>
              <input type="text" name="reps" class="form-control" placeholder="<%= exercise.reps %>"/>
            </div>
            <div class="mb-3">
              <label for="isCompleted" class="form-label">Did you complete your workout?</label>
              <input class="form-check-input ms-2" type="checkbox" name="isCompleted" <% if (exercise.isCompleted) { %> checked <% } %> />
            </div>
            <button type="submit" class="btn btn-primary w-100">Update Exercise</button>
          </form>
        </div>
      </div>
    </div>
  <% }) %>
</div>
</body><br>
<!-- <a class="index-link" href="/workouts">
    <div class="index-text">Back to Workouts</div></a>  -->
</html>